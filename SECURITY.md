# 安全策略

## 支持的版本

| 版本 | 支持状态 |
| --- | --- |
| master 分支最新提交 | 支持（本项目不做长期维护版本，以最新提交为准） |

## 敏感信息红线（贡献者必读）

本项目对接真实教务选课系统，以下内容禁止进入任何 commit：

- 教务账号、学籍号、手机号等个人标识
- 密码、`idToken`、会话 Cookie 的真实值
- `XUANKE_ADMIN_TOKEN`、`SF_API_KEY`、`XUANKE_MASTER_KEY` 的真实值
- `data/.env`、`data/xuanke.db`、`data/.master_key`（已被 .gitignore 忽略）
- 真实抓包文件（HAR，含账号级会话数据）

> 已入库的敏感历史如被发现，请报告并配合重写历史清理（本项目已做过一次全历史脱敏）。

## 报告漏洞

发现安全漏洞（越权、会话伪造、注入、密钥泄露、平台风控规避等），不要公开提交 issue，改为：

- 私信仓库维护者，或
- 走 GitHub 的私有安全披露（Security Advisory）

报告中请说明：漏洞类型、影响范围、复现步骤、建议修复方向。

## 已知安全设计

- **激活码票据单次防重放**：激活码校验前先消费票据，错误激活码会销毁票据、需重新登录。宁可输错重登一次，也不让同一票据在 5 分钟内穷举不同码。
- **管理口令缺失拒绝启动**：`XUANKE_ADMIN_TOKEN` 必须来自环境变量或 `data/.env`，缺失即 `log.Fatal` 退出。
- **凭据 AES-256-GCM 加密入库**：密码与 `vision_key` 均以 `enc:` 密文落库；读到未加密旧明文或损坏主密钥一律拒绝启动。
- **限流与风控**：登录/激活按 IP 分桶限流；网络或风控失败分级退避，不盲目高频重试上游。
- **日志脱敏**：日志只打印 token/密码前 8 位，不输出明文。
