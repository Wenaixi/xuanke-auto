# review-round95 总结（2026-09-23）

## 概述
两个 opus 只读审查代理并行审查全部模块 → 各自写入独立 findings md → 主控逐条深度核实 → 决策。本轮后端 **CRITICAL 0 / MAJOR 0 / MINOR 0 / OBSERVE 1（O95-01）**；前端 **CRITICAL 0 / MAJOR 0 / MINOR 0 / 零新增 OBSERVE**（连续第四十一轮零严重级）。**双端零代码修改需求**——纯观察轮，核心产出：**api 抖动第十轮回落零复现（基线回升）+ 身份防线矩阵第十轮闭合**。

## 审查发现（写入 archive/review-rounds/round95-{backend,frontend}-findings.md）

### 后端（OBSERVE 1 + 3 项延续）
| 编号 | 级别 | 一句话 | 核实裁决 |
|---|---|---|---|
| O95-01 | OBSERVE | api 抖动参考第十轮回落——三轮全量 race 全 11 包全绿、api 包零 connectex；R94 O94-01 低频残余（两轮 1-2 例）本轮归零复现，确认属宿主环境冷启动窗口随机外溢 | ⚠️ **基线回升记录**：CI `-p 1` + 失败重跑吸收口径不变；「八连零复现 → 低频残余 → 归零复现」的波动证明抖动属宿主环境形态锁死而非产品缺陷 |
| 身份防线矩阵延续 | — | 16 项矩阵第十轮逐点 grep（sameClientFor 全部调用点：ProbeForAccount 回写段 :850 + spawnChain 六分支 :1489/:1521/:1551/:1571/:1600/:1635 + maybeRelogin 双侧 :1208/:1254 + ProbeNow/probe() 全局帧语义 + MarkDone/RemoveDone/SubmitAll + Restore）无新裸露写点 | ✅ 闭合 |
| B88-01 持续复核 | — | 回写段 sameClientFor + 双条件识别槽覆盖 + nil 守卫，三钉 + round41 七分支族定向 race 全绿 | ✅ 闭环 |
| O93-02 延续 | OBSERVE | 内嵌 ddddocr 资产 ≈29.7MB 无膨胀（三文件实测 16,048,160 + 13,606,051 + 57,249 字节） | ⚠️ 记录 |
| M87-01 | — | 5s Shutdown 与在飞链注释兜底维持准确 | ✅ 维持 |

### 前端（零新增 OBSERVE / M-1 第三十一轮闭合）
- **M-1 第三十一轮闭合**（四消费点逐字符与 R94 一致无漂移、置位三路径 + 首帧边界完整、读点 9 处无第五消费处）+ 六防保存链零回潮 + **OBSERVE-93-01 第三轮复核**（Button.tsx:42 ring 持续在位零回归、残余面裸按钮 10+ 处清单逐字无漂移）。
- 新视角五项全收敛：组件层级与性能（渲染树深度 ≤4、memo 合理）/ 数据流与状态管理（props 钻取浅、context 无滥用）/ 异常路径 UX（反馈四态齐全、守卫「有解才提示」口径一致）/ 无障碍最后一块（label 覆盖在位、Toast 无显式 aria-live 走读推测 Radix 内建语义可能已覆盖故不立条、prefers-reduced-motion 无分支）/ 定时器事件生命周期（6 处 timer 全 cleanup、2 处监听全成对 remove 零泄漏）。
- OBSERVE-90-01/88-01/85-02/84-01/83-01/77-02/76 族/O-3 族全延续。

## 核实记录（关键）
- **O95-01 基线回升**：对照 R94 O94-01（三轮两轮各 1-2 例 connectex 残余）→ R95 三轮全绿零样本——残余样本全部命中「每个测试新建 httptest server 的首请求」冷启动窗口、定向复测恒绿；「八连零复现 → 低频残余 → 归零复现」的完整周期证明抖动是宿主环境（Windows 回环 TIME_WAIT）的随机外溢而非确定性产品缺陷。**裁决：维持 CI `-p 1` + 失败重跑吸收口径，不推荐夹具改动**（历轮 O86-01 已评估静默窗口不必要）。

## 收尾全量回归
- 后端 `go test -race -count=1 -p 1 -timeout 900s ./...` → 三轮全 11 包全绿（审查代理实测）
- `go build ./...` / `go vet ./...` 零输出 + 定向 race 回归全绿
- 前端 `tsc -b` EXIT 0 + `npm run build` 全量成功 + target-guard 18/18 / admin-auth 6/6 / unauthorized 5/5 + audit.mjs 77 项

## 观察项延续（下轮复核）
后端：O95-01 抖动基线（归零复现后维持观察）/ 身份防线矩阵（第十一轮）/ O93-02 体量记录 / M87-01 窗口留档；前端：M-1 延续管理（第三十二轮）/ OBSERVE-90-01 日志区三态 / 88-01 / 85-02 / 84-01 / 83-01 + 77-02 + 76 族 + O-3 族全表续。

## 教训
1. **「八连零复现 → 低频残余 → 归零复现」的完整周期是抖动定级的终态证据**：O94-01 的低频残余一度像基线退步，R95 三轮全绿证明它只是宿主环境冷启动窗口的随机外溢——**抖动观察需要「多轮实测 + 形态归因」而非单轮样本定级**。残余形态（每个测试新建 httptest 的首请求）自 R80 起从未改变，CI `-p 1` + 失败重跑口径持续吸收。
2. **身份防线矩阵第十轮零新裸露写点**：连续十轮把「网络往返后持锁写点」清单化复核，防线缺口类缺陷的发现面被系统性收窄——B88-01 之后的十轮从未再出现过同族裸露写点，矩阵作为持续防御结构的价值被验证。
3. **前端 41 轮零严重级的观察项管理**：OBSERVE-93-01（Button 焦点）第三轮复核零回归、残余面无漂移；Toast aria-live 走读推测 Radix 内建语义可能已覆盖故「不立条」——**观察项的立条标准随证据充分性动态调整，宁缺毋滥**。